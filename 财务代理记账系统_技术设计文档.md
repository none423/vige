# 财务记账系统 - 技术设计文档

## 目录
1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [数据库设计](#3-数据库设计)
4. [权限系统设计](#4-权限系统设计)
5. [API 接口设计](#5-api-接口设计)
6. [前端页面设计](#6-前端页面设计)
7. [开发规范](#7-开发规范)
8. [实施计划](#8-实施计划)
9. [已实现部分](#9-已实现部分)

---

## 1. 项目概述

### 1.1 系统目标
开发一套**公司内部财务记账系统**，实现：
- 多账套管理（按业务线或部门）
- 会计凭证全流程管理
- 发票管理
- 自动生成财务报表
- 银行对账
- 税务申报辅助

### 1.2 系统角色

| 角色 | 说明 | 权限范围 |
|------|------|---------|
| 系统管理员 | 系统运维 | 全局配置、账户管理 |
| 财务总监 | 审核监督 | 凭证审核、报表审批 |
| 记账会计 | 日常记账 | 凭证录入、发票处理 |
| 出纳 | 资金管理 | 银行对账、资金查看 |
| 部门经理 | 查看报表 | 查看本部门相关报表 |
| 普通员工 | 费用申请 | 费用申请、查看审批状态 |

### 1.3 技术栈

**继承 Vige 现有技术栈**：
- 后端：FastAPI + SQLAlchemy 2.0 + PostgreSQL
- 前端：Vue 2 + iView
- 缓存：Redis
- 任务队列：Huey

---

## 2. 系统架构

### 2.1 模块划分

```
vige-api/vige/api/
├── accounting/          # 💼 账务模块
│   ├── models.py       # 账套、科目、凭证模型
│   ├── api.py        # 账务 API
│   ├── forms.py      # 表单验证
│   └── tasks.py      # 异步任务
├── invoice/           # 📄 发票模块
├── report/          # 📊 报表模块
├── bank/            # 🏦 银行对账
├── tax/             # 💰 税务管理
└── document/         # 📎 档案管理
```

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                      客户端层                    │
├───────────────────┬──────────────────┬───────────────────────┤
│  管理后台  │   vige-bo   │
└───────┬───────┴───────────────────────────────┘
│
┌───────▼───────┐
│    API网关    │
│  /v1/*      │
└───────┬───────┘
│
┌──────────────────┬──────────────────┐
│
┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│  账务管理    │  │  发票管理    │  │  报表管理    │
│  模块       │  │  模块       │  │  模块       │
└───────────────┘  └──────────────┘  └──────────────┘
┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│  银行对账    │  │  税务管理    │  │              │
│  模块       │  │  模块       │  │              │
└───────────────┘  └──────────────┘  └──────────────┘
│
┌──────────────┬──────────────┬───────────────┐
│
┌───────▼────────┐ ┌───────▼───────┐ ┌─────────────────┐
│  PostgreSQL    │ │   Redis    │ │   文件     │
│   数据库    │ │    缓存    │ │   存储     │
└────────────────┘ └───────────────┘ └───────────────┘
```

---

## 3. 数据库设计

### 3.1 ER 图

```
┌────────────────┐       ┌────────────────┐
│  BoRole        │       │  BoUser       │
│  (现有)        │◄──────│  (现有)       │
└────────────────┘       └────────────────┘

┌────────────────┐       ┌────────────────┐
│ AccountSet    │1     N│ Account        │
│  账套          │──────>│  会计科目       │
└───────┬────────┘       └────────────────┘
│
┌────────────────┐
│  Voucher      │
│  记账凭证     │
└───────┬────────┘
│
┌────────────────┐
│VoucherEntry  │
│  凭证分录      │
└────────────────┘

┌────────────────┐
│  Invoice       │
│  发票         │
└────────────────┘
```

### 3.2 数据模型详细设计

#### 3.2.1 账套管理 (AccountSet)

**已实现**

```python
class AccountSet(CRUDMixin, ProfileMixin):
    __tablename__ = 'account_sets'

    name: Mapped[str] = mapped_column(String(100), nullable=False, comment='账套名称')
    code: Mapped[str] = mapped_column(String(50), unique=True, nullable=False, comment='账套编码')
    accounting_standard: Mapped[str] = mapped_column(
        String(50), default='小企业会计准则', comment='会计准则')
    currency: Mapped[str] = mapped_column(String(10), default='CNY', comment='记账本位币')
    status: Mapped[str] = mapped_column(String(20), default='active', comment='状态 active/inactive')
    is_default: Mapped[bool] = mapped_column(Boolean, default=False, comment='是否默认账套')
    current_period: Mapped[Optional[str]] = mapped_column(String(7), nullable=True, comment='当前会计期间 YYYY-MM')
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False, default=datetime.utcnow)
    updated_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True, onupdate=datetime.utcnow)
```

#### 3.2.2 会计科目 (Account)

**已实现**

```python
class Account(CRUDMixin, ProfileMixin):
    __tablename__ = 'accounts'

    account_set_id: Mapped[int] = mapped_column(BigInteger, ForeignKey('account_sets.id'), nullable=False, index=True)
    code: Mapped[str] = mapped_column(String(50), nullable=False, index=True, comment='科目编码')
    name: Mapped[str] = mapped_column(String(100), nullable=False, comment='科目名称')
    name_en: Mapped[Optional[str]] = mapped_column(String(100), nullable=True, comment='英文名称')
    direction: Mapped[str] = mapped_column(String(10), nullable=False, comment='方向 借/贷')
    account_type: Mapped[str] = mapped_column(String(50), nullable=False, comment='科目类别')
    level: Mapped[int] = mapped_column(Integer, default=1, comment='科目级次 1-5')
    parent_id: Mapped[Optional[int]] = mapped_column(BigInteger, ForeignKey('accounts.id'), nullable=True, index=True)
    is_cash_account: Mapped[bool] = mapped_column(Boolean, default=False, comment='现金科目')
    is_bank_account: Mapped[bool] = mapped_column(Boolean, default=False, comment='银行科目')
    has_auxiliary: Mapped[bool] = mapped_column(Boolean, default=False, comment='辅助核算')
    auxiliary_type: Mapped[Optional[str]] = mapped_column(String(50), nullable=True, comment='辅助核算类型')
    is_detail: Mapped[bool] = mapped_column(Boolean, default=True, comment='是否最明细科目')
    is_used: Mapped[bool] = mapped_column(Boolean, default=True, comment='是否启用')
    allow_manual: Mapped[bool] = mapped_column(Boolean, default=True, comment='允许手工录入')
    sort_order: Mapped[int] = mapped_column(Integer, default=0, comment='排序')
```

#### 3.2.3 记账凭证 (Voucher)

**待实现**

```python
class Voucher(CRUDMixin):
    account_set_id: Mapped[int] = mapped_column(BigInteger, ForeignKey('account_sets.id'))
    voucher_no: Mapped[str] = mapped_column(String(50), comment='凭证号（按期间+顺序')
    period: Mapped[str] = mapped_column(String(7), comment='会计期间 YYYY-MM')
    voucher_date: Mapped[date] = mapped_column(Date, comment='凭证日期')
    attach_count: Mapped[int] = mapped_column(Integer, default=0, comment='附件张数')
    source_type: Mapped[str] = mapped_column(String(20), default='manual', comment='来源')
    status: Mapped[str] = mapped_column(String(20), default='pending_entry', comment='状态')
    preparer_id: Mapped[int] = mapped_column(BigInteger, ForeignKey('bo_users.id'))
    reviewer_id: Mapped[Optional[int]] = mapped_column(BigInteger, ForeignKey('bo_users.id'), nullable=True)
    poster_id: Mapped[Optional[int]] = mapped_column(BigInteger, ForeignKey('bo_users.id'), nullable=True)
    summary: Mapped[str] = mapped_column(Text, comment='凭证摘要')
    total_debit: Mapped[Decimal] = mapped_column(DECIMAL(12,2), default=0)
    total_credit: Mapped[Decimal] = mapped_column(DECIMAL(12,2), default=0)
```

#### 3.2.4 凭证分录 (VoucherEntry)

**待实现**

```python
class VoucherEntry(CRUDMixin):
    voucher_id: Mapped[int] = mapped_column(BigInteger, ForeignKey('vouchers.id'))
    account_id: Mapped[int] = mapped_column(BigInteger, ForeignKey('accounts.id'))
    debit_amount: Mapped[Decimal] = mapped_column(DECIMAL(12,2), default=0)
    credit_amount: Mapped[Decimal] = mapped_column(DECIMAL(12,2), default=0)
    auxiliary_type: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)
    auxiliary_id: Mapped[Optional[int]] = mapped_column(BigInteger, nullable=True)
    summary: Mapped[Optional[str]] = mapped_column(String(200), nullable=True)
```

### 3.3 数据库索引设计

```sql
-- 账套索引
CREATE INDEX idx_account_sets_code ON account_sets(code);
CREATE INDEX idx_account_sets_status ON account_sets(status);

-- 科目索引
CREATE INDEX idx_accounts_account_set_id ON accounts(account_set_id);
CREATE INDEX idx_accounts_code ON accounts(code);
CREATE INDEX idx_accounts_parent_id ON accounts(parent_id);
CREATE INDEX idx_accounts_type ON accounts(account_type);
```

---

## 4. 权限系统设计

### 4.1 权限枚举定义

**已实现** - 在 `vige-api/vige/api/constants.py`**

```python
class BoPermission(Enum):
    # 账套管理
    account_set_view = 'account_set_view'
    account_set_manage = 'account_set_manage'

    # 科目管理
    account_view = 'account_view'
    account_manage = 'account_manage'

    # 凭证管理
    voucher_entry = 'voucher_entry'
    voucher_review = 'voucher_review'
    voucher_post = 'voucher_post'
    voucher_delete = 'voucher_delete'
    voucher_print = 'voucher_print'

    # 发票管理
    invoice_view = 'invoice_view'
    invoice_manage = 'invoice_manage'

    # 财务报表
    report_view = 'report_view'
    report_export = 'report_export'

    # 税务管理
    tax_view = 'tax_view'
    tax_manage = 'tax_manage'
```

权限分组：

```python
class BoPermissionGroup(IntEnum):
    accounting = 300      # 账务管理
    voucher = 400         # 凭证管理
    invoice = 500        # 发票管理
    report = 600          # 报表管理
    tax = 700             # 税务管理
```

### 4.2 角色权限配置

| 角色 | 权限组合 |
|------|---------|
| 系统管理员 | 全部权限 |
| 财务总监 | 凭证审核、报表审批、全部查看权限 |
| 记账会计 | 凭证录入、发票管理、银行对账、报表查看 |
| 出纳 | 银行对账、资金查看 |
| 部门经理 | 报表查看 |
| 普通员工 | 费用申请、查看审批状态 |

### 4.3 权限装饰器使用

**已实现**

```python
from .decorators import perm_accepted
from .constants import BoPermission

@app.get('/accounting/account-sets')
@perm_accepted(BoPermission.account_set_view)
def list_account_sets(...):
    ...
```

---

## 5. API 接口设计

### 5.1 账套管理 API

**已实现**

| 方法 | 端点 | 权限 | 说明 |
|------|------|------|
| GET | `/v1/accounting/account-sets` | account_set_view | 账套列表 |
| GET | `/v1/accounting/account-sets/{id}` | account_set_view | 账套详情 |
| POST | `/v1/accounting/account-sets` | account_set_manage | 创建账套 |
| PUT | `/v1/accounting/account-sets/{id}` | account_set_manage | 更新账套 |
| DELETE | `/v1/accounting/account-sets/{id}` | account_set_manage | 删除账套 |
| POST | `/v1/accounting/account-sets/{id}/init-accounts` | account_manage | 初始化科目 |

### 5.2 会计科目 API

**已实现**

| 方法 | 端点 | 权限 | 说明 |
|------|------|------|
| GET | `/v1/accounting/accounts` | account_view | 科目列表 |
| GET | `/v1/accounting/accounts/tree` | account_view | 科目树 |
| GET | `/v1/accounting/accounts/{id}` | account_view | 科目详情 |
| POST | `/v1/accounting/accounts` | account_manage | 创建科目 |
| PUT | `/v1/accounting/accounts/{id}` | account_manage | 更新科目 |
| DELETE | `/v1/accounting/accounts/{id}` | account_manage | 删除科目 |

### 5.3 凭证管理 API

**待实现**

| 方法 | 端点 | 权限 | 说明 |
|------|------|------|
| GET | `/v1/accounting/vouchers` | voucher_view | 凭证列表 |
| GET | `/v1/accounting/vouchers/{id}` | voucher_view | 凭证详情 |
| POST | `/v1/accounting/vouchers` | voucher_entry | 创建凭证 |
| PUT | `/v1/accounting/vouchers/{id}` | voucher_entry | 更新凭证 |
| POST | `/v1/accounting/vouchers/{id}/review` | voucher_review | 审核凭证 |
| POST | `/v1/accounting/vouchers/{id}/post` | voucher_post | 过账凭证 |
| DELETE | `/v1/accounting/vouchers/{id}` | voucher_delete | 删除凭证 |
| GET | `/v1/accounting/vouchers/{id}/print` | voucher_print | 打印凭证 |

### 5.4 财务报表 API

**待实现**

| 方法 | 端点 | 权限 | 说明 |
|------|------|------|
| GET | `/v1/reports/account-balance` | report_view | 科目余额表 |
| GET | `/v1/reports/balance-sheet` | report_view | 资产负债表 |
| GET | `/v1/reports/income-statement` | report_view | 利润表 |
| POST | `/v1/reports/{report_type}/export` | report_export | 导出报表 |

---

## 6. 前端页面设计

### 6.1 页面结构

```
vige-bo/src/view/
├── accounting/                   # 💼 账务管理
│   ├── account-set/
│   │   ├── account-set-list.vue   # 账套列表
│   │   └── account-set-form.vue   # 账套表单
│   ├── account/
│   │   ├── account-list.vue    # 科目列表（树形）
│   │   └── account-form.vue    # 科目表单
│   └── voucher/
│       ├── voucher-list.vue     # 凭证列表
│       ├── voucher-form.vue     # 凭证录入
│       └── voucher-detail.vue   # 凭证详情
├── invoice/                   # 📄 发票管理
├── report/                   # 📊 财务报表
├── bank/                     # 🏦 银行对账
└── tax/                     # 💰 税务管理
```

### 6.2 核心页面设计

**待实现**

---

## 7. 开发规范

### 7.1 后端开发规范

**已实现参考**

```python
from ...db import Base, CRUDMixin, ProfileMixin

class YourModel(CRUDMixin, ProfileMixin):
    __tablename__ = 'your_table_name'

    name: Mapped[str] = mapped_column(String(100), nullable=False)

    def dump(self):
        return {
            'id': self.id,
            'name': self.name,
        }
```

API 路由规范：

```python
from fastapi import Depends, HTTPException
from sqlalchemy.orm import Session
from ...db import sm
from ..decorators import perm_accepted
from ..constants import BoPermission

@router.get('/accounting/account-sets')
@perm_accepted(BoPermission.account_set_view)
def list_account_sets(
    keyword: Optional[str] = Query(None),
    db: Session = Depends(sm.get_db)
):
    q = db.query(AccountSet)
    if keyword:
        q = q.filter(
            (AccountSet.name.ilike(f'%{keyword}%')) | (
            (AccountSet.code.ilike(f'%{keyword}%'))
        )
    account_sets = q.all()
    return dict(success=True, rows=[s.dump() for s in account_sets])
```

### 7.2 数据库迁移规范

**已实现**

迁移文件位置：`vige-api/vige/migrations/versions/xxxxxxxx_add_accounting_tables.py

```bash
# 查看当前迁移
pipenv run alembic current

# 升级数据库
pipenv run alembic upgrade head

# 回滚
pipenv run alembic downgrade -1
```

---

## 8. 实施计划

### 8.1 开发阶段划分

| 阶段 | 模块 | 状态 |
|------|------|------|
| **Phase 1** | 基础框架搭建 | ✅ 已完成 |
| **Phase 2** | 账套与科目管理 | ✅ 已完成 |
| **Phase 3** | 凭证管理 | ⏳ 进行中 |
| **Phase 4** | 发票管理 | ⏳ 待开始 |
| **Phase 5** | 财务报表 | ⏳ 待开始 |
| **Phase 6** | 银行对账 | ⏳ 待开始 |
| **Phase 7** | 税务管理 | ⏳ 待开始 |
| **Phase 8** | 前端页面 | ⏳ 待开始 |
| **Phase 9** | 测试与优化 | ⏳ 待开始 |

### 8.2 里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 | 账套与科目管理 | ✅ 已完成 |
| M2 | 凭证管理 | ⏳ 进行中 |
| M3 | 发票 + 报表 | ⏳ 待开始 |
| M4 | 完整系统 | ⏳ 待开始 |

---

## 9. 已实现部分

### 9.1 已完成的文件

**后端核心模块**

| 文件 | 状态 |
|------|------|
| `vige-api/vige/api/accounting/__init__.py` | ✅ |
| `vige-api/vige/api/accounting/models.py` | ✅ |
| `vige-api/vige/api/accounting/api.py` | ✅ |
| `vige-api/vige/api/constants.py` | ✅ 更新 |
| `vige-api/vige/api/__init__.py` | ✅ 更新 |
| `vige-api/vige/migrations/versions/xxxxxxxx_add_accounting_tables.py` | ✅ |

**文档**

| 文件 | 状态 |
|------|------|
| `财务记账系统_开发总结.md` | ✅ |
| `快速启动指南.md` | ✅ |

### 9.2 快速启动

```bash
# 1. 配置数据库迁移
cd vige-api

# 查看当前迁移状态
pipenv run alembic current

# 2. 更新迁移文件中的 down_revision
# 编辑 xxxxxxxx_add_accounting_tables.py 中的 down_revision 为当前版本

# 3. 执行迁移
pipenv run alembic upgrade head

# 4. 启动服务
make run

# 5. 访问 API 文档
# http://localhost:8000/docs
```

### 9.3 测试建议

1. 创建账套
2. 初始化标准科目
3. 查看科目树
4. 测试科目 CRUD
5. 测试权限控制

---

## 附录

### A. 会计科目体系（小企业会计准则）

```
资产类 (1xxx)
├── 流动资产
│   ├── 1001 库存现金
│   ├── 1002 银行存款
│   ├── 1012 其他货币资金
│   ├── 1123 预付账款
│   ├── 1122 应收账款
│   ├── 1221 其他应收款
│   ├── 1405 库存商品
│   └── 1601 固定资产
└── 非流动资产
    ├── 1602 累计折旧

负债类 (2xxx)
├── 流动负债
│   ├── 2001 短期借款
│   ├── 2202 应付账款
│   ├── 2203 预收账款
│   ├── 2241 其他应付款
│   └── 2221 应交税费

权益类 (3xxx)
├── 3001 实收资本
├── 3002 资本公积
├── 3101 盈余公积
└── 3103 本年利润

成本类 (4xxx)
├── 4001 生产成本
└── 4101 制造费用

损益类 (5xxx)
├── 5001 主营业务收入
├── 5051 其他业务收入
├── 5401 主营业务成本
├── 5402 其他业务成本
├── 5403 营业税金及附加
├── 5601 销售费用
├── 5602 管理费用
└── 5603 财务费用
```

---

*文档版本：2.0
*创建日期：2026-06-08
*最后更新：2026-06-08
