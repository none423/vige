# 快速启动指南

## 🚀 快速上手

### 第一步：配置数据库迁移

在执行迁移前，需要配置迁移文件：

```bash
cd /workspace/vige-api

# 1. 查看当前数据库迁移状态
pipenv run alembic current

# 2. 查看迁移历史，找到最新的迁移 hash
pipenv run alembic history
```

将输出中的最新迁移 hash 记录下来，然后编辑迁移文件：

```bash
# 编辑迁移文件，替换 down_revision
vim vige/migrations/versions/xxxxxxxx_add_accounting_tables.py
```

将 `down_revision = None` 改为：
```python
down_revision = '最新迁移hash'
```

### 第二步：执行数据库迁移

```bash
# 执行迁移
pipenv run alembic upgrade head

# 验证迁移成功
pipenv run alembic current
```

### 第三步：启动后端服务

```bash
# 启动后端（开发模式）
make run

# 或使用 uvicorn
pipenv run uvicorn vige.app:app --reload --port 8000
```

服务启动后访问：
- API：http://localhost:8000
- API 文档：http://localhost:8000/docs

### 第四步：创建第一个账套

通过 API 文档或 curl 创建：

```bash
# 创建账套
curl -X POST http://localhost:8000/v1/accounting/account-sets \
  -H "Content-Type: application/json" \
  -d '{
    "name": "主账套",
    "code": "MAIN001",
    "accounting_standard": "小企业会计准则",
    "currency": "CNY"
  }'
```

### 第五步：初始化标准科目

```bash
# 初始化小企业会计准则科目
curl -X POST http://localhost:8000/v1/accounting/account-sets/1/init-accounts
```

---

## 📋 常用命令

### 数据库操作
```bash
# 查看当前迁移版本
pipenv run alembic current

# 查看迁移历史
pipenv run alembic history

# 升级到最新
pipenv run alembic upgrade head

# 回滚一步
pipenv run alembic downgrade -1

# 重新生成迁移
pipenv run alembic revision --autogenerate -m "description"
```

### 后端操作
```bash
# 安装依赖
make install

# 启动开发服务器
make run

# 运行测试
make test

# 启动 worker
make worker
```

### 前端操作
```bash
# 安装依赖
cd ../vige-bo
yarn install

# 启动开发服务器
yarn dev

# 构建生产版本
yarn build
```

---

## 🔧 常见问题

### 问题 1：迁移失败 - down_revision 不匹配

**错误信息**：
```
AssertionError: Target database is not up to date.
```

**解决方法**：
1. 检查 `alembic_version` 表中的当前版本
2. 确保迁移文件的 `down_revision` 与历史一致

### 问题 2：数据库连接失败

**错误信息**：
```
sqlalchemy.exc.OperationalError: could not connect to server
```

**解决方法**：
1. 确保 PostgreSQL 已启动
2. 检查 `local_config.env` 中的数据库配置
3. 验证数据库用户权限

### 问题 3：API 权限不足

**错误信息**：
```
HTTP 403: Forbidden
```

**解决方法**：
1. 检查用户角色权限配置
2. 确保角色包含相应权限
3. 重新登录获取最新权限

### 问题 4：端口被占用

**错误信息**：
```
OSError: [Errno 98] Address already in use
```

**解决方法**：
1. 查找占用端口的进程
```bash
lsof -i :8000
```
2. 杀死进程或使用其他端口
```bash
uvicorn vige.app:app --reload --port 8001
```

---

## 📚 相关文档

- [财务记账系统_开发总结.md](file:///workspace/财务记账系统_开发总结.md) - 开发总结和下一步计划
- [财务代理记账系统_技术设计文档.md](file:///workspace/财务代理记账系统_技术设计文档.md) - 详细技术设计文档
- [CODE_WIKI.md](file:///workspace/CODE_WIKI.md) - Vige 项目完整 Wiki

---

## 🎯 下一步

1. ✅ 创建账套
2. ✅ 初始化科目
3. ⏳ 配置用户角色
4. ⏳ 开发凭证管理（核心）
5. ⏳ 开发财务报表
6. ⏳ 开发前端页面

查看详细的下一步计划，请阅读 [财务记账系统_开发总结.md](file:///workspace/财务记账系统_开发总结.md)

---

*有问题？查看相关文档或运行 `make help` 查看所有可用命令*
